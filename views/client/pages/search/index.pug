extend ../../layouts/default.pug
include ../../mixins/box_head.pug
include ../../../admin/mixins/alert.pug
include ../../mixins/products_layout.pug

block main 
  .container.my-3
    .row 
      .col-12 
        //- +alertError(5000)
        +box_head("Kết quả tìm kiếm") 
    if(products.length > 0)
      +product-layout-grid(products)
    else 
      h5.tex-center Không tìm thấy kết quả